const React = require('react');
const style = require('./weapon_viewer.scss');

const WeaponViewer = React.createClass({
  render: function() {
    return (<h1>This is awesome</h1>);
  }
});

module.exports = WeaponViewer;
